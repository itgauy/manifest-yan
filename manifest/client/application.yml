apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: recruitment-tracker-client
  namespace: argocd
spec:
  project: recruitment-tracker
  source:
    repoURL: https://github.com/itgauy/manifest-yan.git
    targetRevision: main
    path: manifest/client
  destination:
    server: https://kubernetes.default.svc
    namespace: recruitment-tracker-client
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
